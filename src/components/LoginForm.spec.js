import React from 'react';
import { expect } from 'chai';
import LoginForm from './LoginForm';

describe('<LoginForm />', () => {
  it('1 + 1 should equal 2', () => {
    expect(1).to.equal(1);
  });
});
